# FocalPoint Frontend Environment Configuration
# If left blank or commented out, the frontend automatically uses the built-in edge synthesizer fallback.

# Set this to your deployed Amazon API Gateway endpoint URL (e.g. from `sam deploy` outputs)
# NEXT_PUBLIC_AWS_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod
